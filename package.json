{
  "name": "cw-incentive-app",
  "version": "1.3.0",
  "private": true,
  "description": "TBS Incentive mobile-first web app packaged for Android (v1.3)",
  "engines": { "node": ">=22 <23", "npm": ">=10 <11" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check:syntax": "node scripts/check-syntax.cjs",
    "check:baseline": "node scripts/verify-baseline.cjs",
    "build:web": "node scripts/build-web.cjs",
    "cap:sync": "npm run build:web && cap sync android",
    "android:debug": "npm run cap:sync && node scripts/gradle.cjs assembleDebug",
    "android:release": "npm run cap:sync && node scripts/gradle.cjs assembleRelease",
    "android:aab": "npm run cap:sync && node scripts/gradle.cjs bundleRelease",
    "android:open": "cap open android",
    "verify:platform": "node validation/platform-check.cjs"
  },
  "dependencies": {
    "@capacitor/android": "7.6.9",
    "@capacitor/core": "7.6.9"
  },
  "devDependencies": {
    "@capacitor/cli": "7.6.9"
  }
}
